# ============================================================================
# Streamlit Cloud Secrets Template
# Copy this file and rename to secrets.toml, then fill in your PAT token.
#
# On Streamlit Cloud:
#   Go to your app → Settings → Secrets → paste the contents below
#
# NEVER commit secrets.toml to Git! It's excluded by .gitignore.
# ============================================================================

[github]
pat_token = "YOUR_GITHUB_PAT_TOKEN_HERE"
repo_url = "https://github.com/wstnil/mgm-barcode-app"
